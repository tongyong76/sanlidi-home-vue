export const TOKEN_TIME: string = 'tokenTime';
export const TOKEN_TIME_VALUE: number = 2 * 60 * 60 * 1000; // 60 * 60 * 1000

export const API_URL = import.meta.env.VITE_API_URL + '/u';
export const IMAGE_URL = import.meta.env.VITE_API_URL;

export const site = {
  loginBg: 'food-bg.jpg',
  appId: '',
  appKey: ''
};

// 主页路径，默认使用菜单第一个有效路径，配置后使用此路径
export const HOME_PAGE_PATH = '';
